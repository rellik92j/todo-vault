{
  "name": "todo-vault-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first, Jira-shaped task vault, plus the desktop app over it",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "menu": "tsx scripts/menu.mts",
    "vault": "tsx packages/core/src/cli.ts",
    "mcp": "tsx packages/core/src/mcp-server.ts",
    "seed": "tsx packages/core/scripts/seed-vault.ts",
    "//test": "The glob is load-bearing and needs Node 22. npm runs scripts through cmd.exe on Windows, which does not expand globs, so the pattern reaches Node's test runner intact — and expanding it there is a feature Node gained in 22. apps/desktop does the same. This is what the engines floor below is really about.",
    "test": "tsx --test scripts/*.test.mts && npm run test --workspaces --if-present",
    "typecheck": "tsc --noEmit -p tsconfig.json && npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run build -w todo-vault && npm run dev -w @todo-vault/desktop",
    "//preview": "Building core first is not optional: `start` alone assembles the desktop bundle around whatever packages/core/dist was left containing, which looks like a clean build while carrying a stale core. The sequence lives here rather than in the menu so it holds for anyone typing the command directly — a rule only the menu enforces is not enforced.",
    "preview": "npm run build -w todo-vault && npm run start -w @todo-vault/desktop",
    "preview:skip-build": "npm run start -w @todo-vault/desktop -- --skipBuild",
    "//shortcut": "Writes a desktop .lnk that starts the built app with no terminal, by pointing wscript.exe at scripts/launch.vbs. Deliberately not a build step: the launcher runs what is in apps/desktop/out and puts up a dialog if that is missing, because a double-click has no console to show a ten-second build in and the failure mode is clicking again. `preview` already owns the build-then-launch order; it does not belong in two places. Run this once per clone, and again if the repo moves — a .lnk stores absolute paths.",
    "shortcut": "tsx scripts/shortcut.mts",
    "//update": "Was the three commands the script now runs — `git pull --ff-only && npm install && npm run build -w todo-vault` — and they still run in that order, for the reasons they always did: --ff-only refuses rather than building a surprise merge commit if local and remote diverged, same as start-change; the install runs unconditionally because it is already a fast no-op when nothing moved; and rebuilding core catches a stale one right after pulling rather than partway into launching the app. What a shell chain could not do is the step in front of them. `npm install` writes package-lock.json as well as reading it, so update used to leave the lockfile modified and the *next* update's pull aborted rather than overwrite it — a state that needs recognising and explaining, not a fourth `&&`. scripts/update.mts has the whole argument.",
    "update": "tsx scripts/update.mts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  },
  "//engines": "Was >=20, which nothing had ever tested and which was false: `npm test` globs its test files, and Node only expands those patterns from 22 on. Node 20 also left LTS on 2026-04-30, so the floor moved up to the oldest version that actually runs the suite rather than the suite retreating to the oldest version named here.",
  "engines": {
    "node": ">=22"
  },
  "//allowScripts": "npm 11.16+ blocks dependency install scripts until they are decided here (RFC npm/rfcs#868). esbuild's postinstall is denied rather than allowed: the binary arrives as the @esbuild/<platform> optional dependency, so esbuild loads, transforms and runs its CLI shim with the script blocked. Bare name rather than a pinned version, so a vite or electron-vite bump does not bring the warning back. Must stay in the ROOT package.json — npm ignores this field in a workspace.",
  "allowScripts": {
    "esbuild": false
  }
}
